import React from "react";

import { renderRows } from "../helpers";
import { Content, Card } from "../components";
import data from "../resume";

export default function Labs() {
  return (
    <Content>
      <Card heading={"Re-reduce"}>re-reduce</Card>
    </Content>
  );
}
